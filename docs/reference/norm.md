# Euclidean norm for sparse_numeric via norm()

Euclidean norm for sparse_numeric via norm()

## Usage

``` r
norm(x, ...)

# S4 method for class 'sparse_numeric'
norm(x, ...)
```

## Arguments

- x:

  A `sparse_numeric` vector.

- ...:

  Additional arguments passed to methods (currently ignored).

## Value

Numeric scalar, the Euclidean (L2) norm of `x`.

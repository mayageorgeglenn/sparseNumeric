# L2 norm for sparse_numeric vectors (sparse_norm)

L2 norm for sparse_numeric vectors (sparse_norm)

## Usage

``` r
sparse_norm(x)

# S4 method for class 'sparse_numeric'
sparse_norm(x)
```

## Arguments

- x:

  A `sparse_numeric` vector.

## Value

Numeric scalar, the Euclidean norm of `x`.

# Elementwise addition of sparse_numeric vectors

Elementwise addition of sparse_numeric vectors

## Usage

``` r
sparse_add(x, y, ...)

# S4 method for class 'sparse_numeric,sparse_numeric'
sparse_add(x, y)
```

## Arguments

- x, y:

  Objects of class `sparse_numeric`.

- ...:

  Further arguments passed to methods (currently ignored).

## Value

A `sparse_numeric` vector containing elementwise sums.
